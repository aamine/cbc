import stdio;

int
main(int argc, char **argv)
{
    int[4] a;

    printf("%d;%d;%d;%d", 1 / 1, 2 / 1, 4 / 2, 5 / 2);

    a[0] = 16;
    a[0] /= 1 + 1 * 1 + 1 + 1;
    printf(";%d", a[0]);

    puts("");
    return 0;
}
